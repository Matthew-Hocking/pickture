import { MovieDetails, PaginatedResponse, TVDetails } from '@/app/lib/tmdb/types'
import React from 'react'

const List = (
  results: MovieDetails[]
) => {
  return (
    <div>List</div>
  )
}

export default List